node_modules/
.next/
next-env.d.ts
# Caché incremental de TypeScript: se regenera solo en cada build.
*.tsbuildinfo

# Variables de entorno con claves reales (Groq, Supabase). Solo se versiona .env.example.
.env
.env.local
.env*.local
.claude
text.txt